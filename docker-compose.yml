services:
  drawnix:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 17183:80
    restart: unless-stopped
    volumes:
      - ./data/storage:/storage
    logging:
      options:
        max-size: 1g

